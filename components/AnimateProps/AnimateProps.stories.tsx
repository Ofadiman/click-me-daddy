import type { Meta, StoryObj } from '@storybook/react'
import { AnimateProps } from './AnimateProps'

const meta: Meta<typeof AnimateProps> = {
  title: 'Animate props',
  component: AnimateProps,
}

export default meta

export const Primary: StoryObj<typeof AnimateProps> = {}
